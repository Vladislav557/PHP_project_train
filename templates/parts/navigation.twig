<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/mysite/">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
            {% if currentPage == 'index' %}
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/mysite/">Home</a>
            </li>
            {% else %}
            <li class="nav-item">
                <a class="nav-link"href="/mysite/">Home</a>
            </li>
            {% endif %}
            {% if not is_auth %}
                {% if currentPage == 'login' %}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/mysite/login">Login</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="/mysite/login">Login</a>
                </li>
                {% endif %}
                {% if currentPage == 'registration' %}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/mysite/registration">Registration</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="/mysite/registration">Registration</a>
                </li>
                {% endif %}
            {% endif %}
            {% if is_auth %}
            <li class="nav-item">
                <a class="nav-link" href="/mysite/logout">Logout</a>
            </li>
            {% endif %}
            </ul>
      </div>
    </div>
</nav>